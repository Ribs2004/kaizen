export const metadata = { title: "Stats" };

export default function StatsPage() {
  return (
    <div className="fade-up card-elevated p-6 text-center">
      <div className="mx-auto mb-3 text-3xl">📊</div>
      <h1 className="text-lg font-semibold">Stats — coming soon</h1>
      <p className="mt-1 text-sm text-[var(--foreground-muted)]">
        Per-activity breakdowns and charts are in the backlog.
      </p>
    </div>
  );
}
